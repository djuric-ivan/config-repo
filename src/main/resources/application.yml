spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git: https://github.com/djuric-ivan/config-repo-common

eureka:
  client:
    service-url:
      defaultZone:  http://localhost:8761/eureka/


server:
  port: 9595
